repos:
  - repo: local 
    hooks:
      - id: nb-add-cell-ids
        name: Add missing Jupyter cell IDs
        entry: src/add_cell_ids.py
        language: python
        additional_dependencies:
          - nbformat==5.10.4
        files: \.ipynb$
        types: [jupyter]

  - repo: https://github.com/kynan/nbstripout
    rev: 0.7.1
    hooks:
      - id: nbstripout
        args: [--drop-empty-cells]
# .pre-commit-config.yaml — runs checks/formatters before every `git commit`
# repos:                                   # Top-level list of hook repositories to use
#   - repo: https://github.com/psf/black   # Source repo for the Black formatter hooks
#     rev: 24.8.0                          # Pin exact Black version (reproducible results)
#     hooks:                               # Which hooks from this repo to run
#       - id: black                        # Run Black on staged files with default settings

#   - repo: https://github.com/astral-sh/ruff-pre-commit  # Ruff linter + fixer hooks
#     rev: v0.6.9                          # Pin Ruff hook version
#     hooks:
#       - id: ruff                         # Run Ruff linter
#         args: ["--fix"]                  # Auto-fix issues where safe (keeps diffs small)

#   - repo: https://github.com/kynan/nbstripout  # Notebook output stripper (for clean diffs)
#     rev: 0.7.1                           # Pin nbstripout version
#     hooks:
#       - id: nbstripout                   # Remove .ipynb outputs/metadata from commits

#   - repo: https://github.com/pre-commit/pre-commit-hooks  # Official collection of misc hooks
#     rev: v4.6.0                          # Pin version of the hook bundle
#     hooks:
#       - id: check-ast                    # Ensure Python files parse (no syntax errors)
#       - id: check-yaml                   # Ensure YAML files are valid
#       - id: end-of-file-fixer            # Ensure one trailing newline at file end
#       - id: trailing-whitespace          # Strip trailing spaces at line ends